(function () {
    'use strict';

    angular.module('kms.auth')
            .factory('UserFactory', ['$http', function ($http) {
                    var UserFactory = {};

                    UserFactory.register = function (user, callback, error) {

                    };

                }]);
})();

